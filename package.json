{
  "name": "playwright-ui-automation",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright UI automation portfolio project",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
